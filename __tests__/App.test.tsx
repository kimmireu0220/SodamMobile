/**
 * @format
 */

import 'react-native';
// 통합 테스트는 src/__tests__/integration/App.test.tsx에서 수행됩니다.
// 이 파일은 기본 React Native 프로젝트 템플릿에서 생성된 파일입니다.

describe('App', () => {
  it('has test suite configured', () => {
    expect(true).toBe(true);
  });
});
